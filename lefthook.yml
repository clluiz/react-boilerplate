pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: pnpm eslint {staged_files}
    prettier:
      glob: "*.{js,ts,jsx,tsx,json,css,md,html}"
      run: pnpm prettier --write {staged_files} && git add {staged_files}
